// backend/js/config.js
// Global configuration for Food Ordering System Frontend

// Base URL for all API requests (adjust for production)
const API_BASE = "http://localhost/food-ordering-system/backend/api";

// Optional: Export for module usage (when using type="module")
export { API_BASE };

// For non-module scripts, you can access it as window.API_BASE if needed
// (but recommended to use modules for modern projects)